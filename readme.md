#Demo Labs - Getting a Microsoft SQL server deployed on vSphere 

We are going to 

- Create a new Windows Server VM (Terraform)
- Install Microsoft SQL (Ansible)
- Install Microsoft SQL management Studio (Ansible) 